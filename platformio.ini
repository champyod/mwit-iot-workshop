[platformio]
; Workspace root directing PlatformIO to each activity as a subproject.
; Each activity has its own platformio.ini and builds independently.
; Relative paths are resolved from this file's directory.
src_dir = .
default_envs = esp32dev
